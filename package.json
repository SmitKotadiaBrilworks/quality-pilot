{
  "name": "quality-pilot",
  "version": "1.0.0",
  "description": "AI Test Agent Platform - Agent-based UI automation",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npx turbo run dev",
    "build": "npx turbo run build",
    "start": "npx turbo run start",
    "lint": "npx turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.16",
    "typescript": "^5.3.3"
  }
}
